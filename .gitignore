# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Environment / secrets — commit .env.example, never .env
.env

# Backend runtime data (SQLite DB + WAL files)
backend/data/

# Node / frontend build output
node_modules/
frontend/dist/

# Local tooling caches
.codegraph/
.atl/

# Gentle AI SDD runtime ledger instance marker (local, per-change; never commit)
.gentle-ai-instance

# Impeccable audit reports (local session artifacts)
.impeccable/critique/

# Logs
*.log